.claude
docs/
dist/
static/client.js
node_modules
scoring-sandbox/node_modules/
scoring-sandbox/dist/

# Per-user Claude Code settings (permissions, machine-specific paths)
.claude/settings.local.json

# Ephemeral agent working files (local only)
.claude/todos/

# Built vocab embeddings (large, generated locally)
data/vocab-embeddings.json
data/schelling.db*
data/AuthKey.p8
